import React from 'react';
import ReactDOM from 'react-dom';

console.log('Hello world?');

ReactDOM.render(<p> hi testing 123 I have changed in the time due to vim buffers </p>, document.getElementById('mount')); 
